blueprint:
  name: "Aroma-Link: Full Diffuser Control (Schedule + Night Owl)"
  description: >
    Complete diffuser automation using an evaluate-and-set pattern. Every
    trigger re-evaluates the current state and sets both switches accordingly:

      Schedule active = HVAC running + in scheduled window + home occupied
      Night Owl       = HVAC running + outside schedule + room presence

    Switches are idempotent — redundant on/off calls are safely ignored.
    Your saved schedule configuration is never modified.
  domain: automation
  author: cjam28
  source_url: https://raw.githubusercontent.com/cjam28/homeassistant_aroma-link/master/custom_components/aroma_link_integration/blueprints/automation/aroma_link_full.yaml
  input:
    hvac_entity:
      name: HVAC / Thermostat
      description: >
        The climate entity to monitor. Triggers when hvac_action changes
        between idle and heating/cooling.
      selector:
        entity:
          domain: climate
    occupancy_sensor:
      name: Home Occupancy Sensor
      description: Binary sensor that indicates someone is home (used during scheduled hours).
      selector:
        entity:
          domain: binary_sensor
    presence_sensors:
      name: Room Presence Sensors (After-Hours)
      description: >
        Specific room sensors that detect someone is awake after hours
        (e.g. living room, kitchen). Select one or more.
      selector:
        entity:
          domain: binary_sensor
          multiple: true
    scheduled_on_sensor:
      name: Scheduled On Sensor
      description: "The Aroma-Link 'Scheduled On' binary sensor (binary_sensor.*_scheduled_on)."
      selector:
        entity:
          domain: binary_sensor
    schedule_active_switch:
      name: Schedule Active Switch
      description: "The Aroma-Link 'Schedule Active' switch (switch.*_schedule_active). Controls P1-P4."
      selector:
        entity:
          domain: switch
    night_owl_switch:
      name: Night Owl Switch
      description: "The Aroma-Link 'Night Owl' switch (switch.*_night_owl). Controls P5."
      selector:
        entity:
          domain: switch
    hvac_on_delay:
      name: HVAC On Delay
      description: Minutes to wait after the HVAC starts before enabling the diffuser.
      default: 1
      selector:
        number:
          min: 0
          max: 30
          unit_of_measurement: minutes
    presence_off_delay:
      name: Presence Off Delay
      description: Minutes to wait after all presence sensors clear before disabling Night Owl.
      default: 10
      selector:
        number:
          min: 1
          max: 60
          unit_of_measurement: minutes

mode: restart

triggers:
  - trigger: state
    entity_id: !input hvac_entity
    attribute: hvac_action
    from: "idle"
    for:
      minutes: !input hvac_on_delay
  - trigger: state
    entity_id: !input hvac_entity
    attribute: hvac_action
    to: "idle"
  - trigger: state
    entity_id: !input scheduled_on_sensor
  - trigger: state
    entity_id: !input presence_sensors
    to: "on"
  - trigger: state
    entity_id: !input presence_sensors
    to: "off"
    for:
      minutes: !input presence_off_delay

actions:
  - variables:
      hvac_entity: !input hvac_entity
      scheduled_on_sensor: !input scheduled_on_sensor
      occupancy_sensor: !input occupancy_sensor
      presence_sensors: !input presence_sensors
  - variables:
      hvac_running: >-
        {{ state_attr(hvac_entity, 'hvac_action') not in ['idle', none, 'off'] }}
      scheduled: >-
        {{ is_state(scheduled_on_sensor, 'on') }}
      occupied: >-
        {{ is_state(occupancy_sensor, 'on') }}
      presence: >-
        {{ expand(presence_sensors) | selectattr('state', 'eq', 'on') | list | count > 0 }}
  - variables:
      want_schedule: "{{ hvac_running and scheduled and occupied }}"
      want_night_owl: "{{ hvac_running and (not scheduled) and presence }}"
  - choose:
      - conditions:
          - condition: template
            value_template: "{{ want_schedule }}"
        sequence:
          - action: switch.turn_on
            target:
              entity_id: !input schedule_active_switch
          - action: switch.turn_off
            target:
              entity_id: !input night_owl_switch
      - conditions:
          - condition: template
            value_template: "{{ want_night_owl }}"
        sequence:
          - action: switch.turn_on
            target:
              entity_id: !input night_owl_switch
          - action: switch.turn_off
            target:
              entity_id: !input schedule_active_switch
    default:
      - action: switch.turn_off
        target:
          entity_id: !input schedule_active_switch
      - action: switch.turn_off
        target:
          entity_id: !input night_owl_switch
